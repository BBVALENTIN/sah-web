export enum TipPiesa {
    PION = 'PION',
    TURA = 'TURA',
    CAL = 'CAL',
    NEBUN = 'NEBUN',
    REGINA = 'REGINA',
    REGE = 'REGE'
}

export enum Culoare {
    ALB = 1,
    NEGRU = -1,
    MECI_TERMINAT = 0
}

export enum MessageType {
    CHAT = 'CHAT',
    JOIN = 'JOIN',
    LEAVE = 'LEAVE'
}

export enum LobbyType {
    AVAILABLE = 'AVAILABLE',
    ONGOING = 'ONGOING',
    FINISHED = 'FINISHED',
    ABORTED = 'ABORTED'
}

export enum Formats {
    BLITZ = 'BLITZ',
    CLASSICAL = 'CLASSICAL'
}